{
  "name": "cs4280-webgpu-starter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Starter repository for CS 4280 (Computer Graphics). Plain JS + WebGPU, no framework — the infrastructure for every activity/assignment, with each assignment's graphics algorithm left for you to implement.",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "ci": "npm run lint && npm run build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.4",
    "vite": "^8.1.4"
  }
}
